# Day 3: Functions and Modules

## Topics Covered

- Functions
- Function parameters
- Default arguments
- `*args`
- Return values
- Multiple return values
- Built-in functions
- Variable scope
- Global variables
- `global` keyword
- Creating custom Python modules
- Importing custom modules
- Standard library modules
- `random` module
- `datetime` module
- File handling
- Temperature conversion

## Exercises

1. Simple Interest Calculator — Default Arguments
2. Class Average — `*args`
3. Analyze Numbers — Multiple Return Values and Built-in Functions
4. Shared Booking Counter — Scope and `global` Keyword
5. Temperature Report — Custom Module with `random` and `datetime`

## What I Learned

I learned how to create functions using default arguments, `*args`, return values, and multiple return values. I also learned about variable scope, the `global` keyword, custom modules, and Python standard library modules like `random` and `datetime`.

## Challenges Faced

I found it challenging to understand `*args`, global variables, and how to create and import a custom module. I solved these challenges by testing the functions with different inputs and checking the output.

## How to Run

First look at the file structure because you should be inside the `day-02-functions-and-modules` folder.

Run the Python file or notebook containing the exercises.

If the exercises are saved as Python files, run them using:

```bash
python exercise-01-simple-interest.py